{{!-- 
Це код container файлу для summary сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>
    {{#with main}}
    <main>
        <section>
            {{> title data='My Educations'}}
        <ul>
            {{#each education}}
            <li>
                    {{institution}}
                    {{#if years includeZero=true}} ({{years}}) {{/if}}
                    {{#if isEnd}} (End) {{/if}}
                    {{#unless years includeZero=true}} (не закінчив) {{/unless}}
                    {{#unless years }} {{#if years includeZero=true}} (вчусь!) {{/if}} {{/unless}}
               
                {{!-- {{institution}} ({{years}} years ) {{#if isEnd}} (End) {{/if}} 
                {{#unless years includeZero=true}} (не закінчив) {{/unless}} --}}
        </li>
            {{/each}}
        </ul>
        </section>
        <div>
            {{> title data='My certificates'}}
          <ul>
            {{#each  certificates}}
                <li>
                    {{name}} ({{id}})
                </li>
                {{/each}}

          </ul>
        </div>
    </main>
    {{/with}}

</div>

{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
    Адже наш layout/default.hbs має вже структуру HTML
--}}
