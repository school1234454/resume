{{!-- 
Це код container файлу для summary сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>
    {{#with main}}
    <main>
        <section>
            {{> title data='All skills' big=true}}
        <ul>
            {{#each skills}}
            <li>
                {{name}}
                {{#if point includeZero=true}} ({{point}}) {{/if}}
                {{#if isTop}} (top!) {{/if}}

                {{#unless point includeZero=true}} (Не вивчаемо) {{/unless}}

                {{#unless point}} 
                   {{#if point includeZero=true}}
                   (зараз вивчаемо)
                   {{/if}}
                {{/unless}}
                {{!-- {{name}} ({{point}}) 
                {{#if isTop}} (top) {{/if}}
                {{#unless point includeZero=true}} (Hе вивчаемо) {{/unless}} --}}
            </li>
            {{/each}}
        </ul>
        </section>

        <div>
            {{> title data='My Hobbies'}}
            <ul>
                {{#each hobbies}}
                <li>
                    {{!-- {{name}} ({{isMain}}) --}}
                    {{name}}
                    {{#if isMain includeZero=true}} {{true}} {{/if}}
                    {{#if isMain}} (231231321) {{/if}}                                  
                           

                </li>
                {{/each}}
        
            </ul>
        </div>

    </main>
    {{/with}}

</div>

{{!-- 
    Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
    Адже наш layout/default.hbs має вже структуру HTML
--}}
